name: airflow_dbt_pipeline
version: 1.0.0
config-version: 2

profile: airflow_dbt_pipeline

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

clean-targets:
  - target
  - dbt_packages

models:
  airflow_dbt_pipeline:
    staging:
      +schema: staging
      +materialized: table
    data_mart:
      +schema: data_mart
      +materialized: table
    data_mart_report:
      +schema: data_mart_report
      +materialized: table
